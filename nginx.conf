server {
    listen 80;
    server_name localhost;

    # Serve the built React app
    root /usr/share/nginx/html;
    index index.html;

    # If no static file is found, always serve index.html (React Router will handle the routing)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
